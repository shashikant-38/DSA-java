# DSA-java


#collection :

Java Collections Framework

The Java Collections Framework (JCF) is a set of classes and interfaces that provides a standardized way to store, manage, and manipulate groups of objects dynamically. It includes data structures such as lists, sets, queues, and maps, along with algorithms for searching, sorting, and processing dat
